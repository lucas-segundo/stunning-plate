apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plate-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: k8slearning.duckdns.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: plate-service
                port: 3000
